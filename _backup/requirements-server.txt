# Slim deps for Render web service (server.py only).
# The full requirements.txt is used by GitHub Actions for the daily cron.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pandas>=2.0.0
numpy>=1.24.0

# Auth + payments (see SETUP_ACCOUNTS.md)
supabase>=2.4.0
stripe>=8.0.0
python-dotenv>=1.0.0
itsdangerous>=2.1.0       # used by Starlette SessionMiddleware
